spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tixy
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true  # ← SQL 로그 보기 (선택사항)

  data:
    redis:
      host: localhost
      port: 6379

  cache:
    type: none  # Spring Boot 자동 설정 비활성화

JWT_SECRET_KEY: testsecretkeyfortestjwthellotakocuteverymuch1234567890